import { Record } from "../models/record";

export const get = (): Record[] => {
    //TODO: implementar consulta no firebase

    // try {
    //     const result = JSON.parse(data) as Todo[];
    //     return result;
    // } catch {
    return [];
    // }
}

export const save = (record: Record) => {
    //TODO: salvar record no firebase
    // if (data?.length >= 1)
    //     localStorage.setItem(TODO_STORE, JSON.stringify(data));
}